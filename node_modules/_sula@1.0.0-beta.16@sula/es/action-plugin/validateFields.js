import { rejectSTOP } from '../rope'; // TODO: FormCtx

export var validateFields = function validateFields(ctx) {
  var validateFn = ctx.form.validateFields;
  return validateFn().then(function (values) {
    return values;
  }, rejectSTOP);
};
export var validateGroupFields = function validateGroupFields(ctx, config) {
  var validateFn = ctx.form.validateGroupFields;
  return validateFn.apply(ctx.form, config.args).then(function (values) {
    return values;
  }, rejectSTOP);
};
/**
 * 使用场景：搜索列表
 */

export var validateQueryFields = function validateQueryFields(ctx) {
  return ctx.form.validateFields(true);
};
/**
 * 使用场景：搜索列表
 */

export var resetFields = function resetFields(ctx) {
  return ctx.form.resetFields();
};