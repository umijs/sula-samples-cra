import assign from 'lodash/assign';
import { STOP } from '../rope';
export var modalform = function modalform(ctx, config) {
  var modal = ctx.__modalGetter('modal'); // ctxGetter 给到 sula-form


  return modal.show(assign({}, config, {
    ctxGetter: {
      table: function table() {
        return ctx;
      }
    }
  }));
};
export var drawerform = function drawerform(ctx, config) {
  var modal = ctx.__modalGetter('drawer');

  return modal.show(assign({}, config, {
    ctxGetter: {
      table: function table() {
        return ctx;
      }
    }
  }));
};
export var modalCancel = function modalCancel(ctx) {
  return ctx.modal.modalCancel(STOP);
};
export var modalOk = function modalOk(ctx) {
  return ctx.modal.modalOk(ctx);
};