import sula from '../../core';
export function registerFilterPlugin(pluginName) {
  return function wrapFilter(filterClass) {
    sula.filterType(pluginName, function (ctx) {
      var config = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};
      return new filterClass(ctx, config);
    });
  };
}